# Build
build/
DerivedData/
*.xcarchive
*.ipa

# Généré par XcodeGen (project.yml est la source)
*.xcodeproj

# Secrets — ne JAMAIS committer de credentials
.env
*.credentials.json
auth.json
*token*.json

# Outillage agents (worktrees, reglages locaux) — jamais versionne
.claude/

# Divers
.DS_Store
Thumbs.db
