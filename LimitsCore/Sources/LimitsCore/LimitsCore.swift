/// Placeholder for the LimitsCore package.
///
/// This scaffold (T0.2) only proves the package builds for iOS and macOS. Real models
/// (`UsageSnapshot`, `LimitWindow`, ...), the Claude/Codex usage clients, OAuth, the
/// polling policy, and the snapshot store land in T2.1/T2.2.
public enum LimitsCore {
    /// Marks that the package scaffold exists and compiles; superseded once real
    /// version tracking is needed.
    public static let scaffoldVersion = "0.0.1"
}
